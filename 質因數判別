user_input = int(input("請輸入一個整數:"))

if user_input > 1:
   for i in range(2, user_input):
       if (user_input % i) == 0:
           print(user_input, "是偶數")
           break
   else:
       print(user_input, "是質數")

